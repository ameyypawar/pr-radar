# secrets — never commit
*.env
**/.env
.env
.env.*
!.env.example
!notes/timings.env

# deps / build
node_modules/
dist/
build/
.skybridge/

# local data
infra/data/
*.db
*.log

# supervisor runtime state (logs + pidfile) — infra/supervise.sh
infra/logs/

# os / editor
.DS_Store
.vscode/
.idea/

# claude session exports (contain secrets/passwords) — never commit
infra/*.txt

# Conversation exports — never publish these
20*-*.txt

# Recording notes — not for the public repo
notes/video-script.md
